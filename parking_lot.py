# limitation 

# 平日40 假日60
# 使用率和人流 的關係
# 使用率 參數

###### 討論
# 市調 變動時間區間
# 晚上 使用率高的停車塲 
import model 
import pandas as pd
import 
### 
def past_data() : #(pd.dataframe, int, int, function) average_stop_time, now_price, model.curve
    # call mariadb
     
def prediction() -> list: # return a list of 2 to 8 
    
def save_data():
    # call mariadb
def main( now_use_rate ):
    if now_use_rate > pre:
        new_parameter = (now_use_rate - pre) *  average_stop_time
        model.add_parameter(new_parameter)
        new_price = prediction()
    else:
        new_parameter = (pre - now_use_rate) *  average_stop_time
        model.add_parameter(new_parameter)
        new_price = prediction()
if __init__ == "main":
    